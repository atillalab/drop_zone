# drop_zone.rb

Creates and initializes a shared `Drop Zone` folder in iCloud Drive.

If needed, it creates:

- the `Drop Zone` folder
- a `_README.txt` file inside it

If both already exist, it exits quietly.